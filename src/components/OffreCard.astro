---
const { offre } = Astro.props;
---

<div class="bg-white py-16">
    <div class="flex justify-center">
        <div
            class="offer-card bg-white border-2 border-amber-300 rounded-xl p-6
                   shadow-sm hover:shadow-md hover:-translate-y-1
                   transition-all cursor-pointer w-80"
            data-favori={offre.favori.toString()}
        >
            <img
                src={offre.imgUrl}
                alt={offre.nomMaison}
                class="w-full h-40 object-cover rounded-md mb-4"
            />

            <div class="flex items-center justify-between mb-2">
                <strong class="text-lg text-red-700">
                    {offre.nomMaison}
                </strong>
                <span class="text-red-800 font-bold">
                    {offre.prix} €
                </span>
            </div>

            <p class="text-amber-700 text-sm mb-2">
                {offre.adresse}
            </p>

            <p class="text-sm text-gray-700 mb-4">
                🛏 {offre.nbChambres} chambres • 🛁 {offre.nbSdb} SDB • 📐 {
                    offre.surface
                } m²
            </p>

            <button
                class="favori-btn bg-amber-400 hover:bg-amber-500
                       text-red-900 px-4 py-2 rounded-md
                       text-sm transition-colors w-full"
            >
                Favori
            </button>
        </div>
    </div>
</div>
